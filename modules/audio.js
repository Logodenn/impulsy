const ffmpeg = require('fluent-ffmpeg')
const ytdl = require('ytdl-core')

const getStream = (source, callback) => {
    
}

module.exports = {
    getLocalStream: (options, callback) => {

    },
    getYoutubeStream: (options, callback) => {

    }
}